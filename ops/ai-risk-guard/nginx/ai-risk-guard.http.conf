# HTTP-level prerequisites for the ai-bridge pre-upstream risk guard.
#
# Include this once from the nginx http {} context, before any server/location
# includes that reference ai-risk-guard.location.conf.

lua_shared_dict ai_risk_guard_v8 20m;
